{
  "name": "mume-wiki",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/prebuild.cjs",
    "docs:dev": "npm run prebuild && vitepress dev docs",
    "docs:build": "npm run prebuild && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-vue": "^6.0.7",
    "playwright": "^1.60.0",
    "vitepress": "^1.6.3"
  }
}
